@page "/"
@using Syncfusion.Blazor
@using Syncfusion.Blazor.Navigations
@using Syncfusion.Blazor.Buttons

<SfButton Content="Add tab" OnClick="AddTab" />
<SfTab @ref="sfTab" LoadOn="ContentLoad.Init" ShowCloseButton="true" AllowDragAndDrop="true"
       Height="100%">
</SfTab>

@code {
    SfTab? sfTab;
    async Task AddTab()
    {
        var tab = new TabItem()
            {
                Content = "Hello",
                Header = new TabHeader() { Text = $"Tab {sfTab.Items.Count + 1}", IconPosition = "left" },
            };

        await sfTab.AddTab(new List<TabItem> { tab }, sfTab.Items.Count);
        sfTab.SelectedItem = sfTab.Items.Count - 1;
        this.StateHasChanged();
    }
}